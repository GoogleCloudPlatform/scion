# Scion Hub Settings
# Generated by deploy.sh — do not edit directly.
#
# deploy.sh writes settings.yaml in two stages:
#   Phase 3 (VM Setup):   auth.mode = dev  (SSH tunnel access only)
#   Phase 5 (Finalize):   auth.mode = proxy (IAP-authenticated access)
#
# This template documents the final (Phase 5) configuration.
# deploy.sh generates the actual YAML via heredoc, so this file
# is a reference, not a sed template.
settings_version: "1"
server:
  hub:
    name: "__HUB_NAME__"
  storage:
    local_path: /home/scion/.scion/workspace-storage
  secrets:
    backend: local
  auth:
    mode: proxy
    proxy:
      provider: iap
      iap:
        audience: "__IAP_AUDIENCE__"
  listen_port: 8080
